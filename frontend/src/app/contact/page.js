import FooterInfoPage from '@/components/FooterInfoPage';

export const metadata = {
  title: 'Contact - Neo Karma',
  description: 'Contact Neo Karma.',
};

export default function ContactPage() {
  return (
    <FooterInfoPage
      eyebrow="Support"
      title="Contact"
      description="This Contact page is being prepared. Add email, phone, school coordinator details, and response expectations here when support content is ready."
    />
  );
}
